            {% extends "OCPAppliBundle::layout.html.twig"%}
            {% block title %}{{ parent() }}{% endblock %}
            {% block body %}
                  <div align="center"><h1>Liste des recus </h1></div>
                <div class="well well-small " align="center">
    <table border="1" width="50%"  class="table table-bordered">
         <th>NumTrain</th><th>H2O</th><th>P2O5</th><th>MgO</th><th>SiO2</th> 
        <th>CL-</th> <th>Cd</th><th>modifier</th><th>supprimer</th>
    {% for rc in recu %}
    <tr  class="success"> <td> {{rc.train.numero}}</td><td> {{rc.analyse.h2o}}</td>
        <td>{{rc.analyse.p2o5}}</td><td>{{rc.analyse.mgo}}</td><td>{{rc.analyse.sio2}}</td> 
        <td>{{rc.analyse.cl}}</td> <td>{{rc.analyse.cd}}</td><td><a class="btn btn-info" href="{{ path('ocp_appli_editrecu', {'id': rc.id}) }}">

modifier
</a>
        </td><td>
            <a class="btn btn-danger" href="{{ path('ocp_appli_deleterecu', {'id': rc.id}) }}">Supprimer </a>
       
        
        
        </td>
    
    </tr>
       
    


{% endfor %}  
</table>
</div>
             
            {% endblock %}